package com.example.javalib;

public class MyClass {
    public static void main(String[] args) {

       int a = 10;
        int b = 10;
        //double c = 20;

      //  int total = (int) (c + b); //casting : is we need to translate this num to become right variable

        //total = total + 10;

       // total += 10; // similar to the above one (t = t + 10;)

        //Remainder = what remains
            int total;
           total = a % b;
            System.out.println(total);
        //  System.out.println((c+b)); // also like this
      //    System.out.println("The total is " + total); //concatenation
      //    System.out.println("5 " + total); // + is concat not addition


       /* //Declare variable
        // primitives
        String value = "Welcome";   //type name ="";  same variable names cannot be repeated
        String name = "Hamza";

        //Decimal numbers
        float pi = 3.14f;          // for float declare f in the end of value to know this is a float value
        double pd = 3.14d;        // double d is not necesssay for everytime

        int i = 1023456789;      // there is a limitation in taking nums
        char c = 'A';            // char can be anything like a,.,? but single character
        long l = 1345678900;    // but it is also limited
        byte b = 1;              // max is 127 ranges from -127 to 127
        short s = 123;           // min than int max than byte (short is 2x byte)

        //boolean  true,false,1,0
        boolean isTrue;
        isTrue = true;
        boolean isFalse=false;
        boolean boo = false;

        System.out.println(value);
        System.out.println(name);
        System.out.println(i);
        System.out.println(pi);
        System.out.println(pd);
        System.out.println(c);
        System.out.println(l);
        System.out.println("I am  " + boo + " and my age is " + pi + " my favourite letter is " + c); //concatenation

        //whatever u put in quotes compiler will consider it as string even the "space"

      */

    }

}
